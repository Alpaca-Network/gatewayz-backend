# ============================================
# Full Database Sync Configuration
# ============================================
# Copy this to your .env file or add to Railway/Vercel environment variables
# This ensures your database stays fully synced with all 18k+ models

# Enable automatic sync
PRICING_SYNC_ENABLED=true

# Sync frequency (hours)
# Recommended: 6 hours for production, 2-4 for high-frequency updates
PRICING_SYNC_INTERVAL_HOURS=6

# ALL 30 PROVIDERS - This keeps database fully synced
# Copy this EXACTLY (comma-separated, no spaces):
PRICING_SYNC_PROVIDERS=openrouter,featherless,deepinfra,groq,fireworks,together,cerebras,nebius,xai,novita,chutes,aimo,near,fal,helicone,anannas,aihubmix,vercel-ai-gateway,google-vertex,openai,anthropic,simplismart,onerouter,cloudflare-workers-ai,clarifai,morpheus,sybil,canopywave,modelz,cohere,huggingface

# ============================================
# Quick Setup Instructions
# ============================================
# 1. Copy the above PRICING_SYNC_PROVIDERS value
# 2. Add to your environment:
#    - Local: Update .env file
#    - Railway: railway variables set PRICING_SYNC_PROVIDERS="..."
#    - Vercel: Add via dashboard or vercel.json
# 3. Restart your application
# 4. Run initial sync: curl -X POST https://your-domain.com/admin/model-sync/all
# 5. Verify: curl https://your-domain.com/admin/model-sync/status

# ============================================
# Alternative: Minimal Sync (Original Config)
# ============================================
# If you only want pricing for key providers (saves resources):
# PRICING_SYNC_PROVIDERS=openrouter,featherless,nearai,alibaba-cloud
