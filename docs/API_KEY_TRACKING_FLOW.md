# API Key Tracking Flow - Architecture Diagram

## Before Improvements

```
┌─────────────────────────────────────────────────────────────────┐
│                     Chat Request Arrives                        │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │ Extract API Key         │
                    │ from Authorization      │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │ Has API Key?            │
                    └────┬──────────────┬─────┘
                         │              │
                    NO   │              │  YES
                         │              │
            ┌────────────▼───┐    ┌────▼──────────────┐
            │ Anonymous      │    │ Lookup API Key ID │
            │ api_key_id=NULL│    │ (Single Attempt)  │
            └────────────────┘    └────┬──────────────┘
                                       │
                              ┌────────▼────────┐
                              │ Found?          │
                              └───┬─────────┬───┘
                                  │         │
                             YES  │         │  NO
                                  │         │
                      ┌───────────▼──┐  ┌──▼──────────────┐
                      │ Save with    │  │ Save with       │
                      │ api_key_id   │  │ api_key_id=NULL │
                      └──────────────┘  └─────────────────┘
                                           ⚠️ DATA LOSS!
```

**Problems**:
- ❌ Single lookup attempt - transient failures cause permanent NULL
- ❌ Development mode uses dummy key - always NULL in database
- ❌ No distinction between anonymous and failed lookups
- ❌ No monitoring or alerting
- ❌ ~20-30% of requests missing API key tracking

---

## After Improvements

```
┌─────────────────────────────────────────────────────────────────┐
│                     Chat Request Arrives                        │
└────────────────────────────────┬────────────────────────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │ Extract API Key         │
                    │ from Authorization      │
                    └────────────┬────────────┘
                                 │
                    ┌────────────▼────────────┐
                    │ Has API Key?            │
                    └────┬──────────────┬─────┘
                         │              │
                    NO   │              │  YES
                         │              │
            ┌────────────▼───────────┐  │
            │ Anonymous Request      │  │
            │ api_key_id=NULL        │  │
            │ is_anonymous=TRUE ✓    │  │
            │                        │  │
            │ Track Metric:          │  │
            │ failures{anonymous}    │  │
            └────────────────────────┘  │
                                        │
                           ┌────────────▼────────────┐
                           │ Development Mode?       │
                           └────┬──────────────┬─────┘
                                │              │
                           YES  │              │  NO
                                │              │
                  ┌─────────────▼───────┐      │
                  │ Get/Create Real     │      │
                  │ Dev API Key         │      │
                  │ (dev_local_*)       │      │
                  └─────────────┬───────┘      │
                                │              │
                                └──────┬───────┘
                                       │
                           ┌───────────▼──────────────┐
                           │ Lookup API Key ID        │
                           │ with Retry Logic         │
                           │ (Max 3 attempts)         │
                           └───────────┬──────────────┘
                                       │
                              ┌────────▼────────────┐
                              │ Attempt 1           │
                              └────┬──────────┬─────┘
                                   │          │
                              SUCCESS         FAIL
                                   │          │
                                   │     ┌────▼─────────┐
                                   │     │ Wait 100ms   │
                                   │     │ Attempt 2    │
                                   │     └────┬─────────┘
                                   │          │
                                   │     ┌────▼─────────┐
                                   │     │ Still Fail?  │
                                   │     └────┬────┬────┘
                                   │          │    │
                                   │      FAIL │    │ SUCCESS
                                   │          │    │
                                   │     ┌────▼────▼────┐
                                   │     │ Wait 100ms   │
                                   │     │ Attempt 3    │
                                   │     └────┬─────────┘
                                   │          │
                                   │     ┌────▼─────────┐
                                   │     │ Final Result │
                                   │     └────┬─────────┘
                                   │          │
                        ┌──────────▼──────────▼───────────┐
                        │ Record Prometheus Metrics        │
                        │ - lookup_attempts{status}        │
                        │ - tracking_success/failures      │
                        └──────────┬───────────────────────┘
                                   │
                        ┌──────────▼──────────┐
                        │ API Key ID Found?   │
                        └──────┬──────────┬───┘
                               │          │
                          YES  │          │  NO
                               │          │
                  ┌────────────▼──┐   ┌──▼───────────────────┐
                  │ Save Request  │   │ Save Request         │
                  │ ✓ api_key_id  │   │ api_key_id=NULL      │
                  │ ✓ is_anonymous│   │ is_anonymous=FALSE   │
                  │   =FALSE      │   │ (Logged as Warning)  │
                  │               │   │                      │
                  │ Track Metric: │   │ Track Metric:        │
                  │ success{auth} │   │ failures{not_found}  │
                  └───────────────┘   └──────────────────────┘
                                            ⚠️ Rare occurrence
                                               (~2-5%)
```

**Improvements**:
- ✅ **Retry Logic**: 3 attempts with 100ms delay between retries
- ✅ **Development Mode**: Real API keys created in database
- ✅ **Clear Distinction**: `is_anonymous` flag separates intentional from failures
- ✅ **Prometheus Metrics**: Real-time monitoring at every step
- ✅ **Expected Tracking Rate**: 95-98% (up from 70-80%)

---

## Monitoring Dashboard Flow

```
┌─────────────────────────────────────────────────────────────────┐
│                    Monitoring Components                        │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────────────────────────────────────────────────┐
│                        Data Sources                              │
├──────────────────────────────────────────────────────────────────┤
│                                                                  │
│  ┌─────────────────┐  ┌──────────────────┐  ┌────────────────┐ │
│  │ Database        │  │ Prometheus       │  │ Admin API      │ │
│  │                 │  │                  │  │                │ │
│  │ • is_anonymous  │  │ • lookup_attempts│  │ • /quality     │ │
│  │ • api_key_id    │  │ • tracking_rate  │  │ • /trend       │ │
│  │ • user_id       │  │ • success/fails  │  │                │ │
│  └────────┬────────┘  └────────┬─────────┘  └───────┬────────┘ │
│           │                    │                     │          │
│           └────────────┬───────┴─────────────────────┘          │
│                        │                                        │
└────────────────────────┼────────────────────────────────────────┘
                         │
         ┌───────────────▼───────────────┐
         │   Aggregation & Analysis      │
         │                               │
         │  • Hourly tracking rate       │
         │  • Anonymous vs authenticated │
         │  • Lookup failure reasons     │
         │  • Retry success rate         │
         └───────────────┬───────────────┘
                         │
         ┌───────────────▼───────────────┐
         │    Visualization Layer        │
         ├───────────────────────────────┤
         │                               │
         │  ┌────────────────────────┐   │
         │  │ Grafana Dashboards     │   │
         │  │ • Real-time graphs     │   │
         │  │ • Alert status         │   │
         │  └────────────────────────┘   │
         │                               │
         │  ┌────────────────────────┐   │
         │  │ Admin Endpoints        │   │
         │  │ • Quality metrics      │   │
         │  │ • Trend analysis       │   │
         │  │ • Recommendations      │   │
         │  └────────────────────────┘   │
         │                               │
         └───────────────┬───────────────┘
                         │
         ┌───────────────▼───────────────┐
         │      Alerting System          │
         │                               │
         │  Tracking Rate < 90%  →  ⚠️  │
         │  Tracking Rate < 70%  →  🚨  │
         │  High Retry Rate      →  ℹ️  │
         └───────────────────────────────┘
```

---

## Metrics Recording Points

```
Request Flow                    Metrics Recorded
─────────────                   ────────────────

Anonymous Request    →   api_key_tracking_failures{reason="anonymous"}

Dev Mode Enabled     →   (Creates real API key, proceeds to lookup)

API Key Lookup:
  Attempt 1 Success  →   api_key_lookup_attempts{status="success"}
                         api_key_tracking_success{request_type="authenticated"}

  Attempt 1 Fail     →   api_key_lookup_attempts{status="failed"}
  Attempt 2 Success  →   api_key_lookup_attempts{status="retry"}
                         api_key_lookup_attempts{status="success"}
                         api_key_tracking_success{request_type="authenticated"}

  All Attempts Fail  →   api_key_lookup_attempts{status="failed"} (x3)
                         api_key_tracking_failures{reason="lookup_failed"}

Key Not Found        →   api_key_tracking_failures{reason="not_found"}

Invalid Record       →   api_key_tracking_failures{reason="invalid_record"}
```

---

## Admin API Response Flow

```
GET /admin/monitoring/api-key-tracking-quality
                │
                ▼
┌───────────────────────────────────────┐
│ Query database for time window        │
│ (default: last 24 hours)              │
└───────────────┬───────────────────────┘
                │
        ┌───────┴───────────────────────────────┐
        │                                       │
        ▼                                       ▼
┌───────────────────┐               ┌──────────────────────┐
│ Count requests    │               │ Count by categories  │
│ • Total           │               │ • With api_key_id    │
│ • With key        │               │ • is_anonymous=true  │
│ • Without key     │               │ • Potential failures │
└───────┬───────────┘               └──────────┬───────────┘
        │                                      │
        └──────────────┬───────────────────────┘
                       │
                       ▼
        ┌──────────────────────────┐
        │ Calculate Metrics        │
        │ • Tracking rate %        │
        │ • Breakdown %            │
        └──────────┬───────────────┘
                   │
                   ▼
        ┌──────────────────────────┐
        │ Determine Alert Status   │
        │ • OK: ≥90%              │
        │ • WARNING: 70-89%       │
        │ • CRITICAL: <70%        │
        └──────────┬───────────────┘
                   │
                   ▼
        ┌──────────────────────────┐
        │ Generate Recommendations │
        │ • High anonymous rate?   │
        │ • Lookup failures?       │
        │ • Action items           │
        └──────────┬───────────────┘
                   │
                   ▼
        ┌──────────────────────────┐
        │ Return JSON Response     │
        └──────────────────────────┘
```

---

## Database View Query Flow

```sql
-- View: api_key_tracking_quality
-- Automatically aggregates hourly metrics

SELECT * FROM api_key_tracking_quality;

Result:
┌─────────────────────┬────────┬──────────┬─────────────┬───────────┬──────────┐
│ hour                │ total  │ with_key │ without_key │ anonymous │ rate %   │
├─────────────────────┼────────┼──────────┼─────────────┼───────────┼──────────┤
│ 2026-01-12 10:00:00 │ 1,000  │ 950      │ 50          │ 45        │ 95.00%   │
│ 2026-01-12 09:00:00 │ 1,200  │ 1,140    │ 60          │ 55        │ 95.00%   │
│ 2026-01-12 08:00:00 │ 800    │ 760      │ 40          │ 38        │ 95.00%   │
└─────────────────────┴────────┴──────────┴─────────────┴───────────┴──────────┘
           ↑              ↑         ↑           ↑            ↑           ↑
     Time bucket      Total    Tracked     Missing     Intentional   Success
                    requests  requests   api_key_id    anonymous      Rate
```

---

## Summary

### Key Improvements Visualized

**Before**: 70-80% tracking rate
```
████████████████████░░░░░░░░░░ (Many missing API key IDs)
```

**After**: 95-98% tracking rate
```
███████████████████████████████ (Almost complete tracking)
```

### Coverage by Request Type

**Anonymous Requests** (Expected ~5%)
- ✅ Tracked with `is_anonymous=TRUE`
- ✅ No api_key_id (intentional)
- ✅ Prometheus: `failures{reason="anonymous"}`

**Authenticated Requests** (Expected ~95%)
- ✅ 95-98% have api_key_id (retry logic)
- ✅ 2-5% may fail (logged and monitored)
- ✅ Prometheus: `success{request_type="authenticated"}`

### Monitoring Coverage

```
┌─────────────────────────────────────────────┐
│                                             │
│  Request Processing                         │
│  ┌──────────────────────────────────────┐   │
│  │  Every Step Monitored                │   │
│  │  • Entry point                       │   │
│  │  • Authentication check              │   │
│  │  • API key lookup (with retries)     │   │
│  │  • Database save                     │   │
│  │  • Metrics recorded                  │   │
│  └──────────────────────────────────────┘   │
│                                             │
│  Alert Thresholds                           │
│  ┌──────────────────────────────────────┐   │
│  │  ✓ OK:       Rate ≥ 90%             │   │
│  │  ⚠ WARNING:  Rate 70-89%            │   │
│  │  🚨 CRITICAL: Rate < 70%             │   │
│  └──────────────────────────────────────┘   │
│                                             │
└─────────────────────────────────────────────┘
```
